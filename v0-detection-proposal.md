# v0判定ロジック案（仕様シート4章への回答）

`cases/` の6ケース（OpenFOAM3件・CalculiX3件、正常4件+意図的失敗2件）を
実際に見て、どのファイルのどの記述で判定できるかを整理した。

---

## 1. ソルバー種別（OpenFOAM / CalculiX の自動判別）

フォルダ構造の指紋で判定する（ログの中身に依存しない＝ログが無くても判定可能）。

| ソフト | 判定条件 |
|---|---|
| OpenFOAM | `system/controlDict` ファイルが存在 **かつ** `constant/` ディレクトリが存在 |
| CalculiX | `*.inp` ファイルが直下に存在（Abaqus形式のキーワード入力） |

- 両方に該当 / どちらにも該当しない → 「想定外のソフト」として非表示（仕様6章の方針どおり）。
- 実物確認：`cases/openfoam/*/system/controlDict` は全3件に存在、
  `cases/calculix/*/*.inp` は全3件に存在。相互のフォルダには相手側の指紋ファイルはない。

## 2. ケース名

フォルダ名をそのまま使用（シート記載どおりで変更なし）。
例：`01_icoFoam_cavity`、`01_beamlin_cantilever`

## 3. 実行日

**OpenFOAMのログには実行時刻が埋め込まれている**ことを確認した：

```
Exec   : icoFoam
Date   : Aug 19 2026
Time   : 09:10:48
```

一方 **CalculiXのログ（`log.ccx`）には実行日時の記載が無い**（バージョン情報のみ）。
ソフトによって取得元が非対称になるため、v0では次のいずれかを選ぶ必要がある。

- **案A（推奨）**: 両ソフトとも「結果ファイル群の最も古いmtime（更新日時）」を実行日とする。
  シンプルで実装が1本化でき、ログの有無・書式変更にも影響されない。
  9章の「元ファイルを書き換え・移動しない」方針とも相性が良い（mtimeは保持される）。
- 案B: OpenFOAMはログ内の`Date`/`Time`を解析し、CalculiXのみmtimeにフォールバックする。
  より「正確」だが、ソフトごとに実装が分岐し複雑になる。

→ **案Aで進めてよいか確認したい。** 案Bにしたい場合は指示してほしい。

## 4. 正常終了したか（完走判定のみ）

ソフトごとに「完走の合図」となる文字列が異なることを確認した。

| ソフト | 正常終了の合図 | 異常時の様子（実例で確認） |
|---|---|---|
| OpenFOAM | ログ（`log.<solver名>`）の**最後の非空行が `End`** | 例1: `*.inp`を壊さず`nu`を負値にした`03_icoFoam_cavity_FAILED`では、計算開始直後（t=0.005）にFloating Point Exceptionでクラッシュし、ログは`[stack trace]`セクションで終わる。`End`が無い。 |
| CalculiX | ログ（`log.ccx`）に **`Job finished`** の文字列がある | 例2: 境界条件が存在しない節点を参照するよう壊した`03_beamlin_BROKEN`では、計算が始まる前に`*ERROR reading *BOUNDARY: node 99 is not defined`で停止。`Job finished`が無く、結果ファイル`*.frd`も生成されない。 |

判定ルール（両ソフト共通の考え方）：
- 「完走の合図」がログ末尾/ログ中にあれば ✅ 正常終了
- 無ければ ⚠️ 途中停止・異常終了 として扱う

**「原因のログ行」の見せ方（6章の要望に対応する追加提案）**：
- OpenFOAM: ログ中に `FOAM FATAL ERROR` があればその行、無く `[stack trace]` で
  終わっている場合は「予期しないクラッシュで停止（シグナル終了）」の旨と
  クラッシュ直前の最後の意味のある行（例：`Solving for Ux, ...`）を表示。
- CalculiX: ログ中の最初の `*ERROR` 行をそのまま表示。

なお、CalculiXは「計算開始前の入力エラー」と「計算途中の非収束」でも
挙動が異なる（前者は`.frd`が生成されない／後者は途中までの`.frd`が残る）ため、
将来的に「入力エラーで即停止」と「計算途中で停止」を区別したくなった場合は
`.frd`の有無や`.sta`の増分（INC）件数が使える。v0では区別せず「完走したか否か」
の二値判定のみで良いという理解で進める。

---

## 確認結果（2026-08-19 承認済み）

1. **実行日の取得方法**：案A（mtime統一）で確定。
   将来ログ解析による精度向上が必要になった時点で追加検討する。
   → 追記（2026-08-19）：「最も古いmtime」から「最も新しいmtime」に変更。
   過去ケースをコピーして再実行する運用では、最古のmtimeだとコピー元の
   古いタイムスタンプを拾ってしまうため。詳細はREADME「実行日」の項を参照。
2. **CalculiXの標準サンプル**：`calculix-ccx-test`の標準サンプル集をそのまま
   v0開発・動作確認用データとして採用することで確定。
